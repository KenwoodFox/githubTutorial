import os # Import library os, this will allow us to interact with windows bash commands

print("Hello World") # Print text onto the console, the text inside the ("") Will be printed
print("Cool Beans, because hot beans ain't bueno")
os.system("pause") # Send the pause command to windows, pausing the script before you exit
